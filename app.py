# Need document loader to load the document from website (langchain_community?). Check chunking down webpage. 
# Need ollama to run the LLM model (llama3.1?)
# Need visual representation (streamlit?)
# First retrieve from document (if present), else retrieve information from LLM model?
# 